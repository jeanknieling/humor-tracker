diff --git a/node_modules/react-native-ui-datepicker/lib/commonjs/components/header/next-button.js b/node_modules/react-native-ui-datepicker/lib/commonjs/components/header/next-button.js
index a525b4a..6f3251d 100644
--- a/node_modules/react-native-ui-datepicker/lib/commonjs/components/header/next-button.js
+++ b/node_modules/react-native-ui-datepicker/lib/commonjs/components/header/next-button.js
@@ -30,6 +30,7 @@ const NextButton = ({
   const colorScheme = (0, _reactNative.useColorScheme)();
   const theme = colorScheme ?? 'light';
   const defaultStyles = (0, _react.useMemo)(() => createDefaultStyles(isRTL), [isRTL]);
+  const disabled = calendarView === 'month' || calendarView === 'time';
   const onPress = (0, _react.useCallback)(() => {
     switch (calendarView) {
       case 'day':
@@ -45,14 +46,18 @@ const NextButton = ({
   const iconStyle = (0, _react.useMemo)(() => ({
     ...defaultStyles.icon,
     tintColor: _theme.COLORS[theme].foreground,
-    ...imageStyle
-  }), [imageStyle, theme, defaultStyles.icon]);
+    ...imageStyle,
+    opacity: disabled ? 0.35 : 1
+  }), [imageStyle, theme, defaultStyles.icon, disabled]);
   return /*#__PURE__*/_react.default.createElement(_reactNative.Pressable, {
-    disabled: calendarView === 'time',
+    disabled: disabled,
     onPress: onPress,
     testID: "btn-next",
     accessibilityRole: "button",
-    accessibilityLabel: "Next"
+    accessibilityLabel: "Next",
+    accessibilityState: {
+      disabled
+    }
   }, /*#__PURE__*/_react.default.createElement(_reactNative.View, {
     style: [defaultStyles.iconContainer, defaultStyles.next, style],
     className: className
diff --git a/node_modules/react-native-ui-datepicker/lib/commonjs/components/header/prev-button.js b/node_modules/react-native-ui-datepicker/lib/commonjs/components/header/prev-button.js
index 68abb02..3bb4f83 100644
--- a/node_modules/react-native-ui-datepicker/lib/commonjs/components/header/prev-button.js
+++ b/node_modules/react-native-ui-datepicker/lib/commonjs/components/header/prev-button.js
@@ -30,6 +30,7 @@ const PrevButton = ({
   const colorScheme = (0, _reactNative.useColorScheme)();
   const theme = colorScheme ?? 'light';
   const defaultStyles = (0, _react.useMemo)(() => createDefaultStyles(isRTL), [isRTL]);
+  const disabled = calendarView === 'month' || calendarView === 'time';
   const onPress = (0, _react.useCallback)(() => {
     switch (calendarView) {
       case 'day':
@@ -45,14 +46,18 @@ const PrevButton = ({
   const iconStyle = (0, _react.useMemo)(() => ({
     ...defaultStyles.icon,
     tintColor: _theme.COLORS[theme].foreground,
-    ...imageStyle
-  }), [imageStyle, theme, defaultStyles.icon]);
+    ...imageStyle,
+    opacity: disabled ? 0.35 : 1
+  }), [imageStyle, theme, defaultStyles.icon, disabled]);
   return /*#__PURE__*/_react.default.createElement(_reactNative.Pressable, {
-    disabled: calendarView === 'time',
+    disabled: disabled,
     onPress: onPress,
     testID: "btn-prev",
     accessibilityRole: "button",
-    accessibilityLabel: "Prev"
+    accessibilityLabel: "Prev",
+    accessibilityState: {
+      disabled
+    }
   }, /*#__PURE__*/_react.default.createElement(_reactNative.View, {
     style: [defaultStyles.iconContainer, defaultStyles.prev, style],
     className: className
diff --git a/node_modules/react-native-ui-datepicker/lib/commonjs/components/header/selectors.js b/node_modules/react-native-ui-datepicker/lib/commonjs/components/header/selectors.js
index b932bac..c096340 100644
--- a/node_modules/react-native-ui-datepicker/lib/commonjs/components/header/selectors.js
+++ b/node_modules/react-native-ui-datepicker/lib/commonjs/components/header/selectors.js
@@ -19,10 +19,12 @@ const Selectors = ({
   const {
     mode,
     calendarView,
-    timePicker,
-    showMonthSelector
+    timePicker
   } = (0, _calendarContext.useCalendarContext)();
-  const showMonth = showMonthSelector === true || showMonthSelector !== false && calendarView !== 'year';
+  const showTime = Boolean(timePicker && mode === 'single');
+  const showMonth = calendarView === 'day' || calendarView === 'month';
+  const showYear = calendarView === 'day' || calendarView === 'year';
+  const showTimeButton = showTime && (calendarView === 'day' || calendarView === 'time');
   return /*#__PURE__*/_react.default.createElement(_reactNative.View, {
     style: [defaultStyles.container,
     // eslint-disable-next-line react-native/no-inline-styles
@@ -32,9 +34,7 @@ const Selectors = ({
       justifyContent: 'space-between',
       flexDirection: position === 'left' ? 'row-reverse' : 'row'
     }]
-  }, /*#__PURE__*/_react.default.createElement(_reactNative.View, {
-    style: defaultStyles.monthAndYear
-  }, showMonth && /*#__PURE__*/_react.default.createElement(_monthButton.default, null), /*#__PURE__*/_react.default.createElement(_yearButton.default, null)), timePicker && mode === 'single' && calendarView !== 'year' ? /*#__PURE__*/_react.default.createElement(_timeButton.TimeButton, null) : null);
+  }, showMonth ? /*#__PURE__*/_react.default.createElement(_monthButton.default, null) : null, showYear ? /*#__PURE__*/_react.default.createElement(_yearButton.default, null) : null, showTimeButton ? /*#__PURE__*/_react.default.createElement(_timeButton.TimeButton, null) : null);
 };
 var _default = exports.default = /*#__PURE__*/(0, _react.memo)(Selectors);
 const defaultStyles = _reactNative.StyleSheet.create({
@@ -42,11 +42,6 @@ const defaultStyles = _reactNative.StyleSheet.create({
     flex: 1,
     flexDirection: 'row',
     alignItems: 'center'
-  },
-  monthAndYear: {
-    gap: 5,
-    flexDirection: 'row',
-    alignItems: 'center'
   }
 });
 //# sourceMappingURL=selectors.js.map
diff --git a/node_modules/react-native-ui-datepicker/lib/commonjs/components/header/time-button.js b/node_modules/react-native-ui-datepicker/lib/commonjs/components/header/time-button.js
index 84ef878..f3227b9 100644
--- a/node_modules/react-native-ui-datepicker/lib/commonjs/components/header/time-button.js
+++ b/node_modules/react-native-ui-datepicker/lib/commonjs/components/header/time-button.js
@@ -21,7 +21,8 @@ const TimeButton = () => {
     styles,
     classNames,
     numerals = 'latn',
-    use12Hours
+    use12Hours,
+    components
   } = (0, _calendarContext.useCalendarContext)();
   const {
     hour,
@@ -35,8 +36,17 @@ const TimeButton = () => {
     const minuteLabel = minute < 10 ? `${(0, _utils.formatNumber)(0, numerals)}${(0, _utils.formatNumber)(minute, numerals)}` : `${(0, _utils.formatNumber)(minute, numerals)}`;
     return `${hourLabel}:${minuteLabel} ${use12Hours ? period : ''}`.trim();
   }, [numerals, hour, hour12, minute, use12Hours, period]);
+  const isOpen = calendarView === 'time';
+  const handlePress = () => setCalendarView(isOpen ? 'day' : 'time');
+  if (components !== null && components !== void 0 && components.TimeSelector) {
+    return /*#__PURE__*/_react.default.createElement(_react.default.Fragment, null, components.TimeSelector({
+      text: labelText,
+      isOpen: isOpen,
+      onPress: handlePress
+    }));
+  }
   return /*#__PURE__*/_react.default.createElement(_reactNative.Pressable, {
-    onPress: () => setCalendarView(calendarView === 'time' ? 'day' : 'time'),
+    onPress: handlePress,
     accessibilityRole: "button",
     accessibilityLabel: (0, _dayjs.default)(date || currentDate).format('HH:mm')
   }, /*#__PURE__*/_react.default.createElement(_reactNative.View, {
diff --git a/node_modules/react-native-ui-datepicker/lib/commonjs/datetime-picker.js b/node_modules/react-native-ui-datepicker/lib/commonjs/datetime-picker.js
index 2d053cd..f558b7c 100644
--- a/node_modules/react-native-ui-datepicker/lib/commonjs/datetime-picker.js
+++ b/node_modules/react-native-ui-datepicker/lib/commonjs/datetime-picker.js
@@ -369,37 +369,65 @@ const DateTimePicker = props => {
     }
   }, [mode, timePicker, min, max, timeZone]);
 
-  // set the active displayed month
+  // set the active displayed month and keep the selected date in sync
   const onSelectMonth = (0, _react.useCallback)(value => {
-    const currentMonth = (0, _dayjs.default)(stateRef.current.currentDate).month();
-    const newDate = (0, _dayjs.default)(stateRef.current.currentDate).month(value);
+    const baseDate = stateRef.current.date || stateRef.current.currentDate;
+    const currentMonth = (0, _dayjs.default)(baseDate).month();
+    const newDate = (0, _dayjs.default)(baseDate).month(value);
+    const monthChanged = value !== currentMonth;
 
     // Only call onMonthChange if the month actually changed
-    if (value !== currentMonth) {
+    if (monthChanged) {
       onMonthChange(value);
     }
     dispatch({
       type: _enums.CalendarActionKind.CHANGE_CURRENT_DATE,
       payload: newDate
     });
+    if (monthChanged && mode === 'single' && onChange) {
+      const finalized = timePicker ? _dayjs.default.tz(newDate, timeZone) : _dayjs.default.tz((0, _utils.getStartOfDay)(newDate), timeZone);
+      dispatch({
+        type: _enums.CalendarActionKind.CHANGE_SELECTED_DATE,
+        payload: {
+          date: finalized
+        }
+      });
+      onChange({
+        date: finalized ? (0, _dayjs.default)(finalized).toDate() : finalized
+      });
+    }
     setCalendarView('day');
-  }, [setCalendarView, onMonthChange]);
+  }, [setCalendarView, onMonthChange, mode, onChange, timePicker, timeZone]);
 
-  // set the active displayed year
+  // set the active displayed year and keep the selected date in sync
   const onSelectYear = (0, _react.useCallback)(value => {
-    const currentYear = (0, _dayjs.default)(stateRef.current.currentDate).year();
-    const newDate = (0, _dayjs.default)(stateRef.current.currentDate).year(value);
+    const baseDate = stateRef.current.date || stateRef.current.currentDate;
+    const currentYear = (0, _dayjs.default)(baseDate).year();
+    const newDate = (0, _dayjs.default)(baseDate).year(value);
+    const yearChanged = value !== currentYear;
 
     // Only call onYearChange if the year actually changed
-    if (value !== currentYear) {
+    if (yearChanged) {
       onYearChange(value);
     }
     dispatch({
       type: _enums.CalendarActionKind.CHANGE_CURRENT_DATE,
       payload: newDate
     });
+    if (yearChanged && mode === 'single' && onChange) {
+      const finalized = timePicker ? _dayjs.default.tz(newDate, timeZone) : _dayjs.default.tz((0, _utils.getStartOfDay)(newDate), timeZone);
+      dispatch({
+        type: _enums.CalendarActionKind.CHANGE_SELECTED_DATE,
+        payload: {
+          date: finalized
+        }
+      });
+      onChange({
+        date: finalized ? (0, _dayjs.default)(finalized).toDate() : finalized
+      });
+    }
     setCalendarView('day');
-  }, [setCalendarView, onYearChange]);
+  }, [setCalendarView, onYearChange, mode, onChange, timePicker, timeZone]);
   const onChangeMonth = (0, _react.useCallback)(value => {
     const newDate = (0, _dayjs.default)(stateRef.current.currentDate).add(value, 'month');
     dispatch({
diff --git a/node_modules/react-native-ui-datepicker/lib/module/components/header/next-button.js b/node_modules/react-native-ui-datepicker/lib/module/components/header/next-button.js
index e181fb4..147b712 100644
--- a/node_modules/react-native-ui-datepicker/lib/module/components/header/next-button.js
+++ b/node_modules/react-native-ui-datepicker/lib/module/components/header/next-button.js
@@ -22,6 +22,7 @@ const NextButton = ({
   const colorScheme = useColorScheme();
   const theme = colorScheme ?? 'light';
   const defaultStyles = useMemo(() => createDefaultStyles(isRTL), [isRTL]);
+  const disabled = calendarView === 'month' || calendarView === 'time';
   const onPress = useCallback(() => {
     switch (calendarView) {
       case 'day':
@@ -37,14 +38,18 @@ const NextButton = ({
   const iconStyle = useMemo(() => ({
     ...defaultStyles.icon,
     tintColor: COLORS[theme].foreground,
-    ...imageStyle
-  }), [imageStyle, theme, defaultStyles.icon]);
+    ...imageStyle,
+    opacity: disabled ? 0.35 : 1
+  }), [imageStyle, theme, defaultStyles.icon, disabled]);
   return /*#__PURE__*/React.createElement(Pressable, {
-    disabled: calendarView === 'time',
+    disabled: disabled,
     onPress: onPress,
     testID: "btn-next",
     accessibilityRole: "button",
-    accessibilityLabel: "Next"
+    accessibilityLabel: "Next",
+    accessibilityState: {
+      disabled
+    }
   }, /*#__PURE__*/React.createElement(View, {
     style: [defaultStyles.iconContainer, defaultStyles.next, style],
     className: className
diff --git a/node_modules/react-native-ui-datepicker/lib/module/components/header/prev-button.js b/node_modules/react-native-ui-datepicker/lib/module/components/header/prev-button.js
index 6877602..777bf7c 100644
--- a/node_modules/react-native-ui-datepicker/lib/module/components/header/prev-button.js
+++ b/node_modules/react-native-ui-datepicker/lib/module/components/header/prev-button.js
@@ -22,6 +22,7 @@ const PrevButton = ({
   const colorScheme = useColorScheme();
   const theme = colorScheme ?? 'light';
   const defaultStyles = useMemo(() => createDefaultStyles(isRTL), [isRTL]);
+  const disabled = calendarView === 'month' || calendarView === 'time';
   const onPress = useCallback(() => {
     switch (calendarView) {
       case 'day':
@@ -37,14 +38,18 @@ const PrevButton = ({
   const iconStyle = useMemo(() => ({
     ...defaultStyles.icon,
     tintColor: COLORS[theme].foreground,
-    ...imageStyle
-  }), [imageStyle, theme, defaultStyles.icon]);
+    ...imageStyle,
+    opacity: disabled ? 0.35 : 1
+  }), [imageStyle, theme, defaultStyles.icon, disabled]);
   return /*#__PURE__*/React.createElement(Pressable, {
-    disabled: calendarView === 'time',
+    disabled: disabled,
     onPress: onPress,
     testID: "btn-prev",
     accessibilityRole: "button",
-    accessibilityLabel: "Prev"
+    accessibilityLabel: "Prev",
+    accessibilityState: {
+      disabled
+    }
   }, /*#__PURE__*/React.createElement(View, {
     style: [defaultStyles.iconContainer, defaultStyles.prev, style],
     className: className
diff --git a/node_modules/react-native-ui-datepicker/lib/module/components/header/selectors.js b/node_modules/react-native-ui-datepicker/lib/module/components/header/selectors.js
index 78d34a6..9d702d4 100644
--- a/node_modules/react-native-ui-datepicker/lib/module/components/header/selectors.js
+++ b/node_modules/react-native-ui-datepicker/lib/module/components/header/selectors.js
@@ -10,10 +10,12 @@ const Selectors = ({
   const {
     mode,
     calendarView,
-    timePicker,
-    showMonthSelector
+    timePicker
   } = useCalendarContext();
-  const showMonth = showMonthSelector === true || showMonthSelector !== false && calendarView !== 'year';
+  const showTime = Boolean(timePicker && mode === 'single');
+  const showMonth = calendarView === 'day' || calendarView === 'month';
+  const showYear = calendarView === 'day' || calendarView === 'year';
+  const showTimeButton = showTime && (calendarView === 'day' || calendarView === 'time');
   return /*#__PURE__*/React.createElement(View, {
     style: [defaultStyles.container,
     // eslint-disable-next-line react-native/no-inline-styles
@@ -23,9 +25,7 @@ const Selectors = ({
       justifyContent: 'space-between',
       flexDirection: position === 'left' ? 'row-reverse' : 'row'
     }]
-  }, /*#__PURE__*/React.createElement(View, {
-    style: defaultStyles.monthAndYear
-  }, showMonth && /*#__PURE__*/React.createElement(MonthButton, null), /*#__PURE__*/React.createElement(YearButton, null)), timePicker && mode === 'single' && calendarView !== 'year' ? /*#__PURE__*/React.createElement(TimeButton, null) : null);
+  }, showMonth ? /*#__PURE__*/React.createElement(MonthButton, null) : null, showYear ? /*#__PURE__*/React.createElement(YearButton, null) : null, showTimeButton ? /*#__PURE__*/React.createElement(TimeButton, null) : null);
 };
 export default /*#__PURE__*/memo(Selectors);
 const defaultStyles = StyleSheet.create({
@@ -33,11 +33,6 @@ const defaultStyles = StyleSheet.create({
     flex: 1,
     flexDirection: 'row',
     alignItems: 'center'
-  },
-  monthAndYear: {
-    gap: 5,
-    flexDirection: 'row',
-    alignItems: 'center'
   }
 });
 //# sourceMappingURL=selectors.js.map
diff --git a/node_modules/react-native-ui-datepicker/lib/module/components/header/time-button.js b/node_modules/react-native-ui-datepicker/lib/module/components/header/time-button.js
index 8c93ef3..706565a 100644
--- a/node_modules/react-native-ui-datepicker/lib/module/components/header/time-button.js
+++ b/node_modules/react-native-ui-datepicker/lib/module/components/header/time-button.js
@@ -13,7 +13,8 @@ export const TimeButton = () => {
     styles,
     classNames,
     numerals = 'latn',
-    use12Hours
+    use12Hours,
+    components
   } = useCalendarContext();
   const {
     hour,
@@ -27,8 +28,17 @@ export const TimeButton = () => {
     const minuteLabel = minute < 10 ? `${formatNumber(0, numerals)}${formatNumber(minute, numerals)}` : `${formatNumber(minute, numerals)}`;
     return `${hourLabel}:${minuteLabel} ${use12Hours ? period : ''}`.trim();
   }, [numerals, hour, hour12, minute, use12Hours, period]);
+  const isOpen = calendarView === 'time';
+  const handlePress = () => setCalendarView(isOpen ? 'day' : 'time');
+  if (components !== null && components !== void 0 && components.TimeSelector) {
+    return /*#__PURE__*/React.createElement(React.Fragment, null, components.TimeSelector({
+      text: labelText,
+      isOpen: isOpen,
+      onPress: handlePress
+    }));
+  }
   return /*#__PURE__*/React.createElement(Pressable, {
-    onPress: () => setCalendarView(calendarView === 'time' ? 'day' : 'time'),
+    onPress: handlePress,
     accessibilityRole: "button",
     accessibilityLabel: dayjs(date || currentDate).format('HH:mm')
   }, /*#__PURE__*/React.createElement(View, {
diff --git a/node_modules/react-native-ui-datepicker/lib/module/datetime-picker.js b/node_modules/react-native-ui-datepicker/lib/module/datetime-picker.js
index 1124c40..069a951 100644
--- a/node_modules/react-native-ui-datepicker/lib/module/datetime-picker.js
+++ b/node_modules/react-native-ui-datepicker/lib/module/datetime-picker.js
@@ -361,37 +361,65 @@ const DateTimePicker = props => {
     }
   }, [mode, timePicker, min, max, timeZone]);
 
-  // set the active displayed month
+  // set the active displayed month and keep the selected date in sync
   const onSelectMonth = useCallback(value => {
-    const currentMonth = dayjs(stateRef.current.currentDate).month();
-    const newDate = dayjs(stateRef.current.currentDate).month(value);
+    const baseDate = stateRef.current.date || stateRef.current.currentDate;
+    const currentMonth = dayjs(baseDate).month();
+    const newDate = dayjs(baseDate).month(value);
+    const monthChanged = value !== currentMonth;
 
     // Only call onMonthChange if the month actually changed
-    if (value !== currentMonth) {
+    if (monthChanged) {
       onMonthChange(value);
     }
     dispatch({
       type: CalendarActionKind.CHANGE_CURRENT_DATE,
       payload: newDate
     });
+    if (monthChanged && mode === 'single' && onChange) {
+      const finalized = timePicker ? dayjs.tz(newDate, timeZone) : dayjs.tz(getStartOfDay(newDate), timeZone);
+      dispatch({
+        type: CalendarActionKind.CHANGE_SELECTED_DATE,
+        payload: {
+          date: finalized
+        }
+      });
+      onChange({
+        date: finalized ? dayjs(finalized).toDate() : finalized
+      });
+    }
     setCalendarView('day');
-  }, [setCalendarView, onMonthChange]);
+  }, [setCalendarView, onMonthChange, mode, onChange, timePicker, timeZone]);
 
-  // set the active displayed year
+  // set the active displayed year and keep the selected date in sync
   const onSelectYear = useCallback(value => {
-    const currentYear = dayjs(stateRef.current.currentDate).year();
-    const newDate = dayjs(stateRef.current.currentDate).year(value);
+    const baseDate = stateRef.current.date || stateRef.current.currentDate;
+    const currentYear = dayjs(baseDate).year();
+    const newDate = dayjs(baseDate).year(value);
+    const yearChanged = value !== currentYear;
 
     // Only call onYearChange if the year actually changed
-    if (value !== currentYear) {
+    if (yearChanged) {
       onYearChange(value);
     }
     dispatch({
       type: CalendarActionKind.CHANGE_CURRENT_DATE,
       payload: newDate
     });
+    if (yearChanged && mode === 'single' && onChange) {
+      const finalized = timePicker ? dayjs.tz(newDate, timeZone) : dayjs.tz(getStartOfDay(newDate), timeZone);
+      dispatch({
+        type: CalendarActionKind.CHANGE_SELECTED_DATE,
+        payload: {
+          date: finalized
+        }
+      });
+      onChange({
+        date: finalized ? dayjs(finalized).toDate() : finalized
+      });
+    }
     setCalendarView('day');
-  }, [setCalendarView, onYearChange]);
+  }, [setCalendarView, onYearChange, mode, onChange, timePicker, timeZone]);
   const onChangeMonth = useCallback(value => {
     const newDate = dayjs(stateRef.current.currentDate).add(value, 'month');
     dispatch({
diff --git a/node_modules/react-native-ui-datepicker/lib/typescript/index.d.ts b/node_modules/react-native-ui-datepicker/lib/typescript/index.d.ts
index 9225ee2..ad976d6 100644
--- a/node_modules/react-native-ui-datepicker/lib/typescript/index.d.ts
+++ b/node_modules/react-native-ui-datepicker/lib/typescript/index.d.ts
@@ -1,7 +1,7 @@
 import './locales';
 import './polyfill';
 import DateTimePicker from './datetime-picker';
-export type { DateType, CalendarMode, CalendarDay, CalendarWeek, CalendarMonth, CalendarYear, CalendarComponents, CalendarMonthSelectorProps, CalendarYearSelectorProps, DatePickerBaseProps, } from './types';
+export type { DateType, CalendarMode, CalendarDay, CalendarWeek, CalendarMonth, CalendarYear, CalendarComponents, CalendarMonthSelectorProps, CalendarYearSelectorProps, CalendarTimeSelectorProps, DatePickerBaseProps, } from './types';
 export { useDefaultClassNames, useDefaultStyles } from './theme';
 export default DateTimePicker;
 //# sourceMappingURL=index.d.ts.map
\ No newline at end of file
diff --git a/node_modules/react-native-ui-datepicker/lib/typescript/types.d.ts b/node_modules/react-native-ui-datepicker/lib/typescript/types.d.ts
index b6cb974..8c9246c 100644
--- a/node_modules/react-native-ui-datepicker/lib/typescript/types.d.ts
+++ b/node_modules/react-native-ui-datepicker/lib/typescript/types.d.ts
@@ -99,6 +99,14 @@ export type CalendarYearSelectorProps = {
     /** Toggles the year-picker view */
     onPress: () => void;
 };
+export type CalendarTimeSelectorProps = {
+    /** The formatted time label shown in the header (e.g. "14:30") */
+    text: string;
+    /** Whether the time-picker view is currently open */
+    isOpen: boolean;
+    /** Toggles the time-picker view */
+    onPress: () => void;
+};
 export type CalendarComponents = Partial<{
     /** The component containing the day in the days grid */
     Day: (day: CalendarDay) => React.ReactNode;
@@ -116,6 +124,8 @@ export type CalendarComponents = Partial<{
     MonthSelector: (props: CalendarMonthSelectorProps) => React.ReactNode;
     /** Fully replaces the year selector button in the header */
     YearSelector: (props: CalendarYearSelectorProps) => React.ReactNode;
+    /** Fully replaces the time selector button in the header */
+    TimeSelector: (props: CalendarTimeSelectorProps) => React.ReactNode;
 }>;
 export interface DatePickerBaseProps {
     mode?: CalendarMode;
diff --git a/node_modules/react-native-ui-datepicker/src/components/header/index.tsx b/node_modules/react-native-ui-datepicker/src/components/header/index.tsx
index 21cc77c..5a0fec4 100644
--- a/node_modules/react-native-ui-datepicker/src/components/header/index.tsx
+++ b/node_modules/react-native-ui-datepicker/src/components/header/index.tsx
@@ -1,10 +1,10 @@
-import React, { memo, useMemo } from 'react';
-import { View, StyleSheet } from 'react-native';
-import type { HeaderProps, NavigationProps } from './types';
-import PrevButton from './prev-button';
+import { isEqual } from 'lodash';
+import { memo, useMemo } from 'react';
+import { StyleSheet, View } from 'react-native';
 import NextButton from './next-button';
+import PrevButton from './prev-button';
 import Selectors from './selectors';
-import { isEqual } from 'lodash';
+import type { HeaderProps, NavigationProps } from './types';
 
 const createDefaultStyles = (isRTL: boolean) =>
   StyleSheet.create({
diff --git a/node_modules/react-native-ui-datepicker/src/components/header/next-button.tsx b/node_modules/react-native-ui-datepicker/src/components/header/next-button.tsx
index bc36f13..414201e 100644
--- a/node_modules/react-native-ui-datepicker/src/components/header/next-button.tsx
+++ b/node_modules/react-native-ui-datepicker/src/components/header/next-button.tsx
@@ -42,6 +42,8 @@ const NextButton = ({
   const theme = colorScheme ?? 'light';
   const defaultStyles = useMemo(() => createDefaultStyles(isRTL), [isRTL]);
 
+  const disabled = calendarView === 'month' || calendarView === 'time';
+
   const onPress = useCallback(() => {
     switch (calendarView) {
       case 'day':
@@ -60,17 +62,19 @@ const NextButton = ({
       ...defaultStyles.icon,
       tintColor: COLORS[theme].foreground,
       ...(imageStyle as ImageStyle),
+      opacity: disabled ? 0.35 : 1,
     }),
-    [imageStyle, theme, defaultStyles.icon]
+    [imageStyle, theme, defaultStyles.icon, disabled]
   );
 
   return (
     <Pressable
-      disabled={calendarView === 'time'}
+      disabled={disabled}
       onPress={onPress}
       testID="btn-next"
       accessibilityRole="button"
       accessibilityLabel="Next"
+      accessibilityState={{ disabled }}
     >
       <View
         style={[defaultStyles.iconContainer, defaultStyles.next, style]}
diff --git a/node_modules/react-native-ui-datepicker/src/components/header/prev-button.tsx b/node_modules/react-native-ui-datepicker/src/components/header/prev-button.tsx
index 34475ac..6c798b4 100644
--- a/node_modules/react-native-ui-datepicker/src/components/header/prev-button.tsx
+++ b/node_modules/react-native-ui-datepicker/src/components/header/prev-button.tsx
@@ -42,6 +42,8 @@ const PrevButton = ({
   const theme = colorScheme ?? 'light';
   const defaultStyles = useMemo(() => createDefaultStyles(isRTL), [isRTL]);
 
+  const disabled = calendarView === 'month' || calendarView === 'time';
+
   const onPress = useCallback(() => {
     switch (calendarView) {
       case 'day':
@@ -60,17 +62,19 @@ const PrevButton = ({
       ...defaultStyles.icon,
       tintColor: COLORS[theme].foreground,
       ...(imageStyle as ImageStyle),
+      opacity: disabled ? 0.35 : 1,
     }),
-    [imageStyle, theme, defaultStyles.icon]
+    [imageStyle, theme, defaultStyles.icon, disabled]
   );
 
   return (
     <Pressable
-      disabled={calendarView === 'time'}
+      disabled={disabled}
       onPress={onPress}
       testID="btn-prev"
       accessibilityRole="button"
       accessibilityLabel="Prev"
+      accessibilityState={{ disabled }}
     >
       <View
         style={[defaultStyles.iconContainer, defaultStyles.prev, style]}
diff --git a/node_modules/react-native-ui-datepicker/src/components/header/selectors.tsx b/node_modules/react-native-ui-datepicker/src/components/header/selectors.tsx
index dc6f789..4ae4522 100644
--- a/node_modules/react-native-ui-datepicker/src/components/header/selectors.tsx
+++ b/node_modules/react-native-ui-datepicker/src/components/header/selectors.tsx
@@ -11,12 +11,14 @@ type Props = {
 };
 
 const Selectors = ({ position }: Props) => {
-  const { mode, calendarView, timePicker, showMonthSelector } =
-    useCalendarContext();
+  const { mode, calendarView, timePicker } = useCalendarContext();
 
+  const showTime = Boolean(timePicker && mode === 'single');
   const showMonth =
-    showMonthSelector === true ||
-    (showMonthSelector !== false && calendarView !== 'year');
+    calendarView === 'day' || calendarView === 'month';
+  const showYear = calendarView === 'day' || calendarView === 'year';
+  const showTimeButton =
+    showTime && (calendarView === 'day' || calendarView === 'time');
 
   return (
     <View
@@ -31,13 +33,9 @@ const Selectors = ({ position }: Props) => {
             },
       ]}
     >
-      <View style={defaultStyles.monthAndYear}>
-        {showMonth && <MonthButton />}
-        <YearButton />
-      </View>
-      {timePicker && mode === 'single' && calendarView !== 'year' ? (
-        <TimeButton />
-      ) : null}
+      {showMonth ? <MonthButton /> : null}
+      {showYear ? <YearButton /> : null}
+      {showTimeButton ? <TimeButton /> : null}
     </View>
   );
 };
@@ -50,9 +48,4 @@ const defaultStyles = StyleSheet.create({
     flexDirection: 'row',
     alignItems: 'center',
   },
-  monthAndYear: {
-    gap: 5,
-    flexDirection: 'row',
-    alignItems: 'center',
-  },
 });
diff --git a/node_modules/react-native-ui-datepicker/src/components/header/time-button.tsx b/node_modules/react-native-ui-datepicker/src/components/header/time-button.tsx
index 850eb3f..5b0343e 100644
--- a/node_modules/react-native-ui-datepicker/src/components/header/time-button.tsx
+++ b/node_modules/react-native-ui-datepicker/src/components/header/time-button.tsx
@@ -15,6 +15,7 @@ export const TimeButton = () => {
     classNames,
     numerals = 'latn',
     use12Hours,
+    components,
   } = useCalendarContext();
 
   const { hour, hour12, minute, period } = useMemo(
@@ -38,9 +39,24 @@ export const TimeButton = () => {
     return `${hourLabel}:${minuteLabel} ${use12Hours ? period : ''}`.trim();
   }, [numerals, hour, hour12, minute, use12Hours, period]);
 
+  const isOpen = calendarView === 'time';
+  const handlePress = () => setCalendarView(isOpen ? 'day' : 'time');
+
+  if (components?.TimeSelector) {
+    return (
+      <>
+        {components.TimeSelector({
+          text: labelText,
+          isOpen,
+          onPress: handlePress,
+        })}
+      </>
+    );
+  }
+
   return (
     <Pressable
-      onPress={() => setCalendarView(calendarView === 'time' ? 'day' : 'time')}
+      onPress={handlePress}
       accessibilityRole="button"
       accessibilityLabel={dayjs(date || currentDate).format('HH:mm')}
     >
diff --git a/node_modules/react-native-ui-datepicker/src/datetime-picker.tsx b/node_modules/react-native-ui-datepicker/src/datetime-picker.tsx
index 6dc6d66..860b306 100644
--- a/node_modules/react-native-ui-datepicker/src/datetime-picker.tsx
+++ b/node_modules/react-native-ui-datepicker/src/datetime-picker.tsx
@@ -542,14 +542,16 @@ const DateTimePicker = (
     [mode, timePicker, min, max, timeZone]
   );
 
-  // set the active displayed month
+  // set the active displayed month and keep the selected date in sync
   const onSelectMonth = useCallback(
     (value: number) => {
-      const currentMonth = dayjs(stateRef.current.currentDate).month();
-      const newDate = dayjs(stateRef.current.currentDate).month(value);
+      const baseDate = stateRef.current.date || stateRef.current.currentDate;
+      const currentMonth = dayjs(baseDate).month();
+      const newDate = dayjs(baseDate).month(value);
+      const monthChanged = value !== currentMonth;
 
       // Only call onMonthChange if the month actually changed
-      if (value !== currentMonth) {
+      if (monthChanged) {
         onMonthChange(value);
       }
 
@@ -557,19 +559,37 @@ const DateTimePicker = (
         type: CalendarActionKind.CHANGE_CURRENT_DATE,
         payload: newDate,
       });
+
+      if (monthChanged && mode === 'single' && onChange) {
+        const finalized = timePicker
+          ? dayjs.tz(newDate, timeZone)
+          : dayjs.tz(getStartOfDay(newDate), timeZone);
+
+        dispatch({
+          type: CalendarActionKind.CHANGE_SELECTED_DATE,
+          payload: { date: finalized },
+        });
+
+        (onChange as SingleChange)({
+          date: finalized ? dayjs(finalized).toDate() : finalized,
+        });
+      }
+
       setCalendarView('day');
     },
-    [setCalendarView, onMonthChange]
+    [setCalendarView, onMonthChange, mode, onChange, timePicker, timeZone]
   );
 
-  // set the active displayed year
+  // set the active displayed year and keep the selected date in sync
   const onSelectYear = useCallback(
     (value: number) => {
-      const currentYear = dayjs(stateRef.current.currentDate).year();
-      const newDate = dayjs(stateRef.current.currentDate).year(value);
+      const baseDate = stateRef.current.date || stateRef.current.currentDate;
+      const currentYear = dayjs(baseDate).year();
+      const newDate = dayjs(baseDate).year(value);
+      const yearChanged = value !== currentYear;
 
       // Only call onYearChange if the year actually changed
-      if (value !== currentYear) {
+      if (yearChanged) {
         onYearChange(value);
       }
 
@@ -577,9 +597,25 @@ const DateTimePicker = (
         type: CalendarActionKind.CHANGE_CURRENT_DATE,
         payload: newDate,
       });
+
+      if (yearChanged && mode === 'single' && onChange) {
+        const finalized = timePicker
+          ? dayjs.tz(newDate, timeZone)
+          : dayjs.tz(getStartOfDay(newDate), timeZone);
+
+        dispatch({
+          type: CalendarActionKind.CHANGE_SELECTED_DATE,
+          payload: { date: finalized },
+        });
+
+        (onChange as SingleChange)({
+          date: finalized ? dayjs(finalized).toDate() : finalized,
+        });
+      }
+
       setCalendarView('day');
     },
-    [setCalendarView, onYearChange]
+    [setCalendarView, onYearChange, mode, onChange, timePicker, timeZone]
   );
 
   const onChangeMonth = useCallback(
diff --git a/node_modules/react-native-ui-datepicker/src/index.ts b/node_modules/react-native-ui-datepicker/src/index.ts
index da2a104..eb99f65 100644
--- a/node_modules/react-native-ui-datepicker/src/index.ts
+++ b/node_modules/react-native-ui-datepicker/src/index.ts
@@ -12,6 +12,7 @@ export type {
   CalendarComponents,
   CalendarMonthSelectorProps,
   CalendarYearSelectorProps,
+  CalendarTimeSelectorProps,
   DatePickerBaseProps,
 } from './types';
 export { useDefaultClassNames, useDefaultStyles } from './theme';
diff --git a/node_modules/react-native-ui-datepicker/src/types.ts b/node_modules/react-native-ui-datepicker/src/types.ts
index 1fc1a31..f8d7277 100644
--- a/node_modules/react-native-ui-datepicker/src/types.ts
+++ b/node_modules/react-native-ui-datepicker/src/types.ts
@@ -136,6 +136,15 @@ export type CalendarYearSelectorProps = {
   onPress: () => void;
 };
 
+export type CalendarTimeSelectorProps = {
+  /** The formatted time label shown in the header (e.g. "14:30") */
+  text: string;
+  /** Whether the time-picker view is currently open */
+  isOpen: boolean;
+  /** Toggles the time-picker view */
+  onPress: () => void;
+};
+
 export type CalendarComponents = Partial<{
   /** The component containing the day in the days grid */
   Day: (day: CalendarDay) => React.ReactNode;
@@ -153,6 +162,8 @@ export type CalendarComponents = Partial<{
   MonthSelector: (props: CalendarMonthSelectorProps) => React.ReactNode;
   /** Fully replaces the year selector button in the header */
   YearSelector: (props: CalendarYearSelectorProps) => React.ReactNode;
+  /** Fully replaces the time selector button in the header */
+  TimeSelector: (props: CalendarTimeSelectorProps) => React.ReactNode;
 }>;
 
 export interface DatePickerBaseProps {
